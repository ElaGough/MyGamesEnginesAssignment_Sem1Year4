/* Ela Gough 
 * Games Engines
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//used for Color.lerp. Needed to get Color from Image Script
//[RequireComponent(typeof(Image))]

public class AudioColor : AudioTriggerSpectrum
{
    /*//Renderer rend;
    public Color beatColor;
    public Color restColor;
    public float duration = 1.0f;

    void Start()
    {

    }

    void Update()
    {
        if (_isBeat)
        {
            float lerp = Mathf.PingPong(Time.time, duration) / duration;
            this.GetComponent<Image>().color = Color.Lerp(restColor, beatColor, lerp);
        }
        else if (!_isBeat)
        {
            float lerp = Mathf.PingPong(Time.time, duration) / duration;
            this.GetComponent<Image>().color = Color.Lerp(beatColor, restColor, lerp);
        }

    }*/


   
}
